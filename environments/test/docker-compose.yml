version: "3"
services:

  #####################
  ### Tests runner  ###
  #####################
  tests-runner:
    build:
      context: ../..
      dockerfile: environments/test/Dockerfile
      args:
        REQUIREMENTS: requirements/requirements-test.txt
    env_file: ../../.env
    volumes:
      - "../../:/code"


volumes:
  blog-data: {}
